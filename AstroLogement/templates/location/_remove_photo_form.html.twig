<form method="post" action="{{ path('app_location_photo_remove', {'id': location.id, 'photo': index}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('remove' ~ location.id) }}">
    <button class="btn">Remove</button>
</form>
